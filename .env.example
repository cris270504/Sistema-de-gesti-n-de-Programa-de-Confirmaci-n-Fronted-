# Copiar a .env.local (dev) o configurar en el panel de Vercel (prod).

# API Laravel (solo prod; en dev se usa el proxy /api de Vite).
VITE_API_URL=

# Supabase Auth (Fase 1 de la migración). En dev apunta al stack local:
#   VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
